Run the Kotlin/Gradle test suite. Run:

```bash
./gradlew test
```

To run a specific test class:

```bash
./gradlew test --tests "com.example.MyTest"
```

Always fix failing tests before opening a PR.
