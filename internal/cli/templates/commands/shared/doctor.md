Diagnose common issues with the VibeWarden sidecar and dev environment. Run:

```bash
vibew doctor
```

This checks Docker availability, port conflicts, TLS certificate validity, sidecar config, and upstream app reachability, then prints a summary of any problems found with suggested fixes.
