# Removed-artifact registry for VibeWarden.
# Each line is the literal name of an artifact (file, command, etc) that
# used to exist but no longer does. The CI grep guard at
# internal/quality/removed_artifacts_test.go fails when these names
# appear outside the allowlisted paths (CHANGELOG.md, decisions/).
# Add new entries when you remove a verb, file, or other named artifact.
deploy.sh
