# ADR-093: bundle image-name resolution — cwd-basename fallback unified across `vibew bundle` and `--build`

**Status**: Removed 2026-05-04 — was not an architectural decision (bug fix: two image-name derivation chains diverged; PM-locked Option A and explicitly described as "fix at the leaf, not the source"; failed the ADR threshold retroactively).

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
