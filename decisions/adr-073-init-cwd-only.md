# ADR-073: Make `vibew init` scaffold in current directory only

**Status**: Removed 2026-05-04 — was not an architectural decision (CLI ergonomics simplification; self-described as "pre-adoption so impact is minimal"; pure CLI UX change with no domain or port impact; failed the ADR threshold retroactively).

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
