# ADR-497: Graceful Shutdown / Connection Draining

**Status**: Removed 2026-05-04 — this file captures no architectural decision. The issue (#497) asked "do we have graceful shutdown?" and the answer was "yes, already implemented". The body describes existing wiring with no decision made. Additionally, number 497 is almost certainly a mis-paste of the issue number (#497); it is wildly out of sequence with the rest of the ADR series (001–102). No external references cite "ADR-497" as a stable handle.

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
