# ADR-059: Rename `vibew init` to `vibew wrap` for sidecar-only scaffolding

**Status**: Removed 2026-05-04 — was not an architectural decision (the command rename was purely cosmetic; no changes to scaffolded output or behaviour; no domain or port impact; failed the ADR threshold retroactively).

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
