# ADR-078: Wire acme_email to Single-Site Caddy ACME Issuer

**Status**: Removed 2026-05-04 — was not an architectural decision (adding an `Email` field and wiring it into the ACME issuer JSON object; bug fix where single-site silently dropped what multi-site already had; failed the ADR threshold retroactively).

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
