# ADR-014: Prometheus Fallback Exporter for Backward Compatibility

**Status**: Demoted to docs 2026-05-04 — see `docs/internal/telemetry-migration.md`. Content superseded by ADR-012 / ADR-015 (Prometheus fallback adapter retired when OTel SDK migration completed).

This ADR has been relocated. The decision content was preserved verbatim at the
target path. Numbering remains stable; existing PR / commit references continue
to resolve to this stub.
