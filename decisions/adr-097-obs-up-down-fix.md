# ADR-097: Fix `vibew obs up` no-op and `vibew obs down` nuking the main stack

**Status**: Removed 2026-05-04 — was not an architectural decision (adapter + template fix; two regressions from PR #1149 fixed in one PR; pure bug fix; failed the ADR threshold retroactively).

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
