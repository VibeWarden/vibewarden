# ADR-094: vibew bundle sensitive-file awareness block

**Status**: Removed 2026-05-04 — was not an architectural decision (CLI/output concern only; no new entities, value objects, domain events, or ports; the detection helper is a private function in `internal/cli/cmd/`; failed the ADR threshold retroactively).

See `decisions/README.md` for the active ADR index. Numbering remains stable; existing references continue to resolve to this tombstone.
